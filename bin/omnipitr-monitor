#!/usr/bin/perl
use strict;

use Cwd qw(abs_path);
use FindBin;
use lib abs_path( "$FindBin::Bin/../lib" );

use OmniPITR::Program::Monitor;

OmniPITR::Program::Monitor->new()->run();

# vim: set ft=perl :
