#!/usr/bin/env perl
use strict;

use Cwd qw(abs_path);
use FindBin;
use lib abs_path( "$FindBin::Bin/../lib" );

use OmniPITR::Program::Checksum;

OmniPITR::Program::Checksum->new()->run();

# vim: set ft=perl :
