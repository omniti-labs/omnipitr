#!/usr/bin/perl
use strict;

use Cwd qw(abs_path);
use FindBin;
use lib abs_path( "$FindBin::Bin/../lib" );

use OmniPITR::Backup::Master;

OmniPITR::Backup::Master->new()->run();

# vim: set ft=perl :
