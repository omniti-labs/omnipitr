#!/usr/bin/env perl
use strict;

use Cwd qw(abs_path);
use FindBin;
use lib abs_path( "$FindBin::Bin/../lib" );

use OmniPITR::Program::Synch;

OmniPITR::Program::Synch->new()->run();

# vim: set ft=perl :
